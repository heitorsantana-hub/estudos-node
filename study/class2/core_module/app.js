//Core Modules são as bibliotecas já existentes no NODE, e para usar-las é necessário fazer o requirimento

const path = require("path");
const extension = path.extname("arquivo.txt");

console.log(extension);
